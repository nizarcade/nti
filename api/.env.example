# Server
APP_ENV=development
APP_HOST=0.0.0.0
APP_PORT=8090
ALLOWED_ORIGINS=http://localhost:3030,http://127.0.0.1:3030

# Database (SQLite default — switch to postgresql+psycopg for prod)
DATABASE_URL=postgresql://neondb_owner:npg_cnaLyjMoq25B@ep-broad-pond-aqextx2q-pooler.c-8.us-east-1.aws.neon.tech/neondb?sslmode=require&channel_binding=require

# Stripe (test keys)
STRIPE_SECRET_KEY=sk_test_...
STRIPE_PUBLISHABLE_KEY=pk_test_...
STRIPE_WEBHOOK_SECRET=whsec_...
# Optional: pre-created recurring Price IDs by amount in cents (JSON map).
# If unset, monthly donations create an inline recurring price per checkout.
STRIPE_MONTHLY_PRICE_MAP={}

# PayPal (sandbox by default)
PAYPAL_CLIENT_ID=
PAYPAL_CLIENT_SECRET=
PAYPAL_BASE_URL=https://api-m.sandbox.paypal.com

# Donation receipt
RECEIPT_FROM_EMAIL=info@ntiafrica.org
ORG_LEGAL_NAME=Northern Transformation Initiative
ORG_ADDRESS=P.O. Box 14271-00100, Nairobi, Kenya

# Frontend public URLs (used as Stripe success/cancel redirects)
PUBLIC_WEB_URL=http://localhost:3030

# Contact form destination
CONTACT_TO_EMAIL=info@ntiafrica.org

# SMTP (optional — leave blank to just log)

SMTP_HOST=smtp.gmail.com
EMAIL_USE_TLS=True
EMAIL_USE_SSL=False
SMTP_PORT=587
SMTP_USER='uniminu36@gmail.com'
SMTP_FROM='uniminu36@gmail.com'
SMTP_PASSWORD='dyam jsir jaiy fxwj'