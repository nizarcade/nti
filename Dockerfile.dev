FROM node:20-alpine
WORKDIR /app

COPY package.json package-lock.json* ./
RUN npm ci

# Source comes in via bind mount in docker-compose.dev.yml
EXPOSE 3030
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0", "--port", "3030"]
